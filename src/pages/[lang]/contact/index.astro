---
import { Contact } from '../../../page-content';
import T from '../../../assets/i18n/de.json';

export function getStaticPaths() {
  return [
    {params: {lang: 'de'}},
    {params: {lang: 'en'}}
  ];
}

---

<Contact T={T.CONTACT}/>