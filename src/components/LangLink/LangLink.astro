---
interface Props {
    href: string,
    className?: string,
    transitionName?: string,
}

const { href, className, transitionName } = Astro.props;
const { lang = '' } = Astro.params;
const langLink = lang ? `/${lang}${href}` : href;
---

<a
    href={langLink}
    data-astro-prefetch
    class={className}
    transition:name={transitionName}
>
    <slot/>
</a>